class enemy_test:
	def __init__(self, color, pos, start_pos, radius, width, moving, speed, health):
		self.color = color
		self.pos = pos
		self.start_pos = start_pos
		self.radius = radius
		self.width = width
		self.speed = speed
		self.health = health
	 